server:
  port: 8080

management:
  server:
    port: 8081
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      base-path: /
      exposure:
        include: liveness,health,prometheus
      path-mapping:
        health: readiness
        prometheus: metrics
